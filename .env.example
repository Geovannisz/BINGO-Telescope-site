# This is an example of the environment variables used by the BINGO Telescope Site
# Copy this file to `.env` and adjust the values according to your environment.

# Target deployment domain used for canonical URLs and SEO JSON-LD structured data
# Default (if not set): https://bingotelescope.com
SITE_URL=https://bingotelescope.com

# Base path for hosting in subdirectories (e.g. GitHub Pages without CNAME)
# Note: For custom domains (CNAME), leave this empty or set to `/`
# BASE_PATH=/BINGO-Telescope-site/
